<!DOCTYPE html>
<html lang="es">
<head>
  <meta charset="UTF-8">
  <title>Registro</title>
  <link rel="stylesheet" href="/css/estilos.css">
  <style>
    body {
      font-family: 'Arial', sans-serif;
      background: linear-gradient(to right, #f8f9fa, #e9ecef);
      display: flex;
      justify-content: center;
      align-items: center;
      height: 100vh;
      margin: 0;
    }
    .register-container {
      background-color: #fff;
      padding: 40px 50px;
      border-radius: 10px;
      box-shadow: 0 4px 20px rgba(0, 0, 0, 0.1);
      width: 350px;
      text-align: center;
    }
    .register-container h2 {
      margin-bottom: 20px;
      color: #495057;
    }
    .register-container input {
      width: 100%;
      padding: 12px;
      margin: 10px 0;
      border: 1px solid #ced4da;
      border-radius: 5px;
      font-size: 14px;
    }
    .register-container button {
      width: 100%;
      padding: 12px;
      background-color: #007bff;
      color: #fff;
      border: none;
      border-radius: 5px;
      cursor: pointer;
      font-size: 16px;
      transition: background-color 0.3s ease;
    }
    .register-container button:hover {
      background-color: #0056b3;
    }
    .register-container .secondary-button {
      background-color: #6c757d;
      margin-top: 10px;
    }
    .register-container .secondary-button:hover {
      background-color: #5a6268;
    }
    .register-container .terms {
      text-align: left;
      margin-top: 15px;
    }
    .register-container .terms input {
      width: auto;
      margin-right: 10px;
    }
    .error-message {
      color: #dc3545;
      margin-top: 10px;
    }
  </style>
</head>
<body>
  <div class="register-container">
    <h2>Registro</h2>
    <form action="/register" method="POST" onsubmit="return validateForm()">
      <input type="text" name="user" placeholder="Usuario" required>
      <input type="email" name="email" placeholder="Correo Electrónico" required>
      <input type="password" name="password" placeholder="Contraseña" required>
      <input type="password" name="r_password" placeholder="Repite Contraseña" required>
      <div class="terms">
        <input type="checkbox" id="terms" name="terms" required>
        <label for="terms">Acepto los <a href="/terms">Términos y Condiciones</a></label>
      </div>
      <button type="submit">Registrar</button>
    </form>
    <button class="secondary-button" onclick="window.location.href='/login'">Ya tengo una cuenta</button>
    <% if (error) { %>
      <p class="error-message"><%= error %></p>
    <% } %>
  </div>
  <script>
    function validateForm() {
      const terms = document.getElementById('terms');
      if (!terms.checked) {
        alert('Debes aceptar los Términos y Condiciones para registrarte.');
        return false;
      }
      return true;
    }
  </script>
</body>
</html>
